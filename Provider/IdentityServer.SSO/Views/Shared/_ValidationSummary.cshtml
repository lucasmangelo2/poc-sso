@if (ViewContext.ModelState.IsValid == false)
{
    <div class="alert alert-danger">
        <strong>Atenção, verifique o formulário</strong>
        <div asp-validation-summary="All" class="danger"></div>
    </div>
}